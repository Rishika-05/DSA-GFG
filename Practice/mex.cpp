#include<bits/stdc++.h>
using namespace std;

int main()
{
    int t;
    cin>>t;
    while(t>0)
    {
        long n,k;
        cin>>n>>k;
        vector <long> v(n);
        for(int i=0;i<n;i++)
        {
            cin>>v[i];
        }
        sort(v.begin(),v.end());
        unordered_set<long> s;
        long mex=0,max=0;
        for(int i=0;i<k;i++)
        {
            for(int i=0;i<n;i++)
            {
                
            }
        }
        t--;
    }
    return 0;
}