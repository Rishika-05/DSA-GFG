#include<bits/stdc++.h>
using namespace std;

int main()
{
    string s="geeksforgeeks";
    cout<<s.find("geeks")<<"\n";
    cout<<s.find("nor")<<"\n";
    cout<<s.substr(1,2)<<"\n";                  // substr(starting index, length)
}